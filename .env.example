# -----------------------------------------------------------------------------
# 2brain Configuration
# -----------------------------------------------------------------------------

# --- SHARED SECURITY ---
# A random string used to sign/verify JWT tokens.
# Must be the same for both Frontend and Backend.
# Generate one with: `openssl rand -base64 32`
AUTH_SECRET=changeme_to_a_long_random_string

# Comma-separated list of email addresses allowed to log in and access data.
ALLOWED_USERS=alice@example.com,bob@example.com


# --- BACKEND (FastAPI) ---

# PostgreSQL connection string.
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/2brain

# OpenAI API Key for embedding queries (text-embedding-3-small).
OPENAI_API_KEY=sk-proj-...

# Admin API Key for CLI scripts and Cron jobs (bypasses JWT auth).
ADMIN_API_KEY=changeme_admin_secret

# Optional: Vector search tuning (IVFFLAT probes).
# Higher = more accurate but slower. Default: 10.
IVFFLAT_PROBES=10


# --- FRONTEND (SvelteKit) ---

# OAuth Provider Credentials (GitHub example).
# Create an OAuth App at https://github.com/settings/developers
GITHUB_ID=your_github_client_id
GITHUB_SECRET=your_github_client_secret

# Internal URL for the frontend server to reach the backend (Server-side fetch).
# In local dev: http://127.0.0.1:8100
# In Docker/Railway: http://backend-service-name:8100
API_URL=http://127.0.0.1:8100

# Public URL for the browser to reach the backend (Client-side fetch).
# In production, this might be https://api.yourdomain.com
PUBLIC_API_BASE=http://localhost:8100

# The public URL of the frontend application.
# Required by SvelteKit for CSRF protection in production.
ORIGIN=http://localhost:5173
